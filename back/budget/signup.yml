info:
  name: signup
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/v1/user/signup
  body:
    type: json
    data: |
      {
        "name": "ug",
        "email": "y5",
        "password": "y1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
