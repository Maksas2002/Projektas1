info:
  name: signup
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5000/users/signup
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
