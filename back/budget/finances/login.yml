info:
  name: login
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/api/v1/user/login
  body:
    type: json
    data: |-
      {
        "email": "",
        "password": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
