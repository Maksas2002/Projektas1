info:
  name: signup
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/v1/user/signup
  body:
    type: json
    data: |-
      {
        "name":"admin", 
        "email":"adminas@mail.com", 
        "password":"adminas"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
