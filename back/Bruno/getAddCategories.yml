info:
  name: getAddCategories
  type: http
  seq: 10

http:
  method: POST
  url: http://localhost:3000/api/v1/admin/categories
  body:
    type: json
    data: |-
      {
        "name": "Utilities",
        "type": "expense"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
