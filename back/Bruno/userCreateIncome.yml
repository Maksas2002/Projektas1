info:
  name: userCreateIncome
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:3000/api/v1/user/income/3/add
  body:
    type: json
    data: |-
      {
        "amount" : "100.00",
        "description": "test test test",
        "date": "2026-10-10"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
