info:
  name: login
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/v1/user/login
  body:
    type: json
    data: |-
      { 
        "email":"balevicius.arminas@gmail.com", 
        "password":"arminas"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
