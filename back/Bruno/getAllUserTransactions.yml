info:
  name: getAllUserTransactions
  type: http
  seq: 16

http:
  method: GET
  url: http://localhost:3000/api/v1/history
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
