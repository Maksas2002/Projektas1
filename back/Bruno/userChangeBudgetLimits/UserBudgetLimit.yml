info:
  name: UserBudgetLimit
  type: http
  seq: 1

http:
  method: PATCH
  url: http://localhost:3000/api/v1/user/3/my-budgets/2025-05-01/update
  body:
    type: json
    data: |-
      {
        "amount_limit": 10
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
