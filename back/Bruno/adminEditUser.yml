info:
  name: adminEditUser
  type: http
  seq: 7

http:
  method: PATCH
  url: http://localhost:3000/api/v1/admin/users/3
  body:
    type: json
    data: |-
      {
        "name":"adminTop", 
        "email":"adminastop@mail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
