info:
  name: login
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/v1/users/login
  body:
    type: json
    data: |
      {
        "email":"user@gmail.com",
        "password":"pipi9996"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
